<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_logo_citrus_mix"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_logo_citrus_mix_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SipNSavor">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity-alias
            android:name=".MainActivityCitrusMix"
            android:roundIcon="@mipmap/ic_logo_citrus_mix_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_citrus_mix"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityBlackberry"
            android:roundIcon="@mipmap/ic_logo_blackberry_mix_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_blackberry_mix"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityColdCoffee"
            android:roundIcon="@mipmap/ic_logo_cold_coffee_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_cold_coffee"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityDark"
            android:roundIcon="@mipmap/ic_logo_dark_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_dark"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityLemonade"
            android:roundIcon="@mipmap/ic_logo_lemonade_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_lemonade"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

        <activity-alias
            android:name=".MainActivityWildBerriesMix"
            android:roundIcon="@mipmap/ic_logo_wild_berries_mix_round"
            android:label="@string/app_name"
            android:icon="@mipmap/ic_logo_wild_berries_mix"
            android:enabled="false"
            android:targetActivity=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity-alias>

    </application>

</manifest>